services:
  gateway:
    build:
      context: ../gateway
    ports:
      - "8080:8080"
    environment:
      - RUNTIME_NODE_URL=http://runtime-node:9090
  runtime-node:
    build:
      context: ../runtime-node
    ports:
      - "9090:9090"
